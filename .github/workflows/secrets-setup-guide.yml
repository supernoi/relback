name: Secrets Setup Guide

on:
  workflow_dispatch:

jobs:
  guide:
    runs-on: ubuntu-latest
    steps:
      - name: Print required secrets
        run: |
          echo "Configure repository secrets before enabling deploy workflows:"
          echo "- PROJECTS_AUTOMATION_TOKEN"
          echo "- STAGING_HOST"
          echo "- STAGING_SSH_KEY"
